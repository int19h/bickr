{
	"name": "@bickr/agent-runtime",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"check:bundle": "wrangler deploy --dry-run --config wrangler.jsonc --env test --outdir .wrangler/build-check",
		"cf-typegen": "wrangler types --config wrangler.jsonc",
		"deploy": "wrangler deploy --config wrangler.deploy.jsonc",
		"deploy:test": "wrangler deploy --config wrangler.jsonc --env test",
		"dev": "wrangler dev --config wrangler.jsonc"
	},
	"dependencies": {
		"@bickr/shared": "0.1.0"
	}
}

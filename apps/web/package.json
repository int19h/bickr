{
	"name": "@bickr/web",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "vite build && node ../../scripts/check-pages-static-config.mjs",
		"cf-typegen": "wrangler types --config wrangler.jsonc",
		"deploy": "npm run build && wrangler pages deploy dist/client --project-name=bickr",
		"deploy:test": "npm run build && wrangler pages deploy dist/client --project-name=bickr --branch=test --commit-dirty=true",
		"dev": "vite",
		"dev:full": "npm run build && wrangler pages dev dist/client -c wrangler.jsonc -c ../../workers/agent-runtime/wrangler.jsonc -c ../../workers/forum-coordinator/wrangler.jsonc",
		"preview": "npm run build && wrangler pages dev dist/client -c wrangler.jsonc"
	},
	"dependencies": {
		"@bickr/shared": "0.1.0",
		"oauth4webapi": "^3.8.5",
		"react": "^19.2.5",
		"react-dom": "^19.2.5"
	},
	"devDependencies": {
		"vite-plugin-pwa": "^1.3.0",
		"workbox-build": "^7.4.1",
		"workbox-window": "^7.4.1"
	}
}
